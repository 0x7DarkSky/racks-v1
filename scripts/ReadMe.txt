Workflow normal pour la suite

À chaque nouveau produit :

node scripts/add-product.js
git add .
git commit -m "add product"
git push

Si tu veux, on peut maintenant améliorer le script pour qu’il gère mieux les images Amazon.